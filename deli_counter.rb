# Write your code here.
katz_deli=[]

def line(line)
  if line.length == 0
    "The line is currently empty."
  else
  
end
end

def take_a_number(line, name)
  line.push(name)
  puts ("Hello #{name}, you are number #{line.length} in line.")
end